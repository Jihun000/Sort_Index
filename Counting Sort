#include <iostream>

using namespace std;

int index[10001];

int main()
{
	int max;

	scanf("%d", &max);

	for (int i = 0; i < max; i++)
	{
		int num;
		scanf("%d", &num);
		index[num]++;
	}
	for (int i = 1; i <= 10000; i++)
	{
		for (int j = 0; j < index[i]; j++)
		{
			printf("%d\n", i);
		}
	}
}
